apiVersion: v1
kind: Service
metadata:
  name: worker
  namespace: llm-platform
  labels:
    app: worker
    app.kubernetes.io/name: worker
    app.kubernetes.io/part-of: llm-platform
spec:
  type: ClusterIP
  selector:
    app: worker
  ports:
    - name: http
      port: 8002
      targetPort: 8002
      protocol: TCP
