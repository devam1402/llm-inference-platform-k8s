apiVersion: v1
kind: Service
metadata:
  name: cost-tracker
  namespace: llm-platform
  labels:
    app: cost-tracker
    app.kubernetes.io/name: cost-tracker
    app.kubernetes.io/part-of: llm-platform
spec:
  type: ClusterIP
  selector:
    app: cost-tracker
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
